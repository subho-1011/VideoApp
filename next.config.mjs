/** @type {import('next').NextConfig} */
const nextConfig = {
    // darkMode: ["class"],
    images: {
        domains: ["res.cloudinary.com", "images.unsplash.com"],
    },
};

export default nextConfig;
